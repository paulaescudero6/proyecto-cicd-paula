pipeline {
    agent any
    stages {
        stage('Test de Paula') {
            steps {
                // Esto le ordena a Jenkins ejecutar tu test
                sh 'python3 test_app.py'
            }
        }
    }
}
